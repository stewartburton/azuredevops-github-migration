###############################
# Azure DevOps to GitHub Migration Tool
# .env example template
###############################

# Required Tokens
# Azure DevOps PAT scopes: Code (Read), Work Items (Read), Project and Team (Read) at minimum.
AZURE_DEVOPS_PAT=your_azure_devops_personal_access_token_here

# GitHub PAT scopes: repo, workflow (if migrating workflows), admin:repo_hook (optional if webhooks), project (if using classic projects)
GITHUB_TOKEN=your_github_personal_access_token_here

# Optional Defaults (these can also be supplied in config/config.json)
# AZURE_DEVOPS_ORG=your-ado-org
# GITHUB_ORG=your-github-org

# Performance / Rate Limiting Tuning
# MIGRATION_MAX_CONCURRENT_REPOS=4
# MIGRATION_API_SLEEP_SECONDS=0.2

# Logging Level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO

# Proxy Settings (if required)
# HTTPS_PROXY=https://user:pass@proxy.example.com:8443
# HTTP_PROXY=http://user:pass@proxy.example.com:8080

# If set to 1 will enable extra verbose HTTP debug (may leak secrets to logs) – use cautiously
# HTTP_DEBUG=0

# End of file