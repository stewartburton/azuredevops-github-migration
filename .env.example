###############################
# Azure DevOps to GitHub Migration Tool
# .env example template (minimal)
###############################

# Required Tokens
# Azure DevOps PAT scopes (minimum): Code (Read), Work Items (Read), Project and Team (Read)
AZURE_DEVOPS_PAT=your_azure_devops_personal_access_token_here

# GitHub PAT scopes: repo, workflow (for pipelines), admin:repo_hook (optional), project (if classic projects)
GITHUB_TOKEN=your_github_personal_access_token_here

# Azure DevOps organization slug (no URL). Alias supported: AZURE_DEVOPS_ORG
AZURE_DEVOPS_ORGANIZATION=your_azure_devops_org_here

# GitHub organization or user owner for destination repositories. Alias: GITHUB_ORG
GITHUB_ORGANIZATION=your_github_org_here

# Optional Defaults
# MIGRATION_MAX_CONCURRENT_REPOS=4
# MIGRATION_API_SLEEP_SECONDS=0.2
# LOG_LEVEL=INFO
# HTTPS_PROXY=https://user:pass@proxy.example.com:8443
# HTTP_PROXY=http://user:pass@proxy.example.com:8080
# HTTP_DEBUG=0

# End of file